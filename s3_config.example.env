# S3 Configuration for Article Scraper
# Copy this file to .env or set these environment variables

# Enable S3 upload (set to 'true' to upload articles to S3 instead of saving locally)
S3_UPLOAD_ENABLED=false

# S3 Bucket Configuration
S3_BUCKET_NAME=your-article-scraper-bucket
AWS_REGION=us-east-1

# AWS Credentials (alternative methods below)
# Method 1: Environment variables (not recommended for production)
AWS_ACCESS_KEY_ID=your-access-key-id
AWS_SECRET_ACCESS_KEY=your-secret-access-key

# Alternative credential methods:
# Method 2: AWS credentials file (~/.aws/credentials)
# Method 3: IAM role (if running on EC2)
# Method 4: AWS SSO

# Optional: S3 Key Prefix (will be auto-generated with timestamp if not set)
# S3_KEY_PREFIX=articles/custom-prefix

# Configuration Notes:
# 1. When S3_UPLOAD_ENABLED=true, articles will be uploaded to S3 instead of saved locally
# 2. The PowerShell script will skip downloading files when S3 is enabled
# 3. Ensure your AWS credentials have the following S3 permissions:
#    - s3:PutObject
#    - s3:PutObjectAcl
#    - s3:ListBucket
# 4. Articles will be organized in S3 with the following structure:
#    s3://bucket-name/articles/YYYYMMDD_HHMMSS/article-name.json
#    s3://bucket-name/articles/YYYYMMDD_HHMMSS/all_articles.csv
#    s3://bucket-name/articles/YYYYMMDD_HHMMSS/scrape_summary.json
